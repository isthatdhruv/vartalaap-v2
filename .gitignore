# Rust
/target
**/target
# Cargo.lock IS committed. These crates build shipped binaries, so the lock
# file is what makes a tagged release build the same dependency versions that
# were tested, instead of whatever resolves on CI that day.

# Node
node_modules
dist
.vite

# Tauri
src-tauri/target
src-tauri/gen

# Misc
.DS_Store
*.log
.env
.env.*

# Built installers
packaging/out/

# Flatpak build artifacts
packaging/flatpak/vartalaap.deb
packaging/flatpak/build/
packaging/flatpak/repo/
packaging/flatpak/.flatpak-builder/
.superpowers/
graphify-out/

# A 4.3 GB backup zip of this repo lives in the working tree. GitHub rejects
# any file over 100 MB, so committing it would fail the push outright.
*.zip
